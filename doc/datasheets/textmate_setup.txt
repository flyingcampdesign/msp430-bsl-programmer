To get the top level input files (i.e. datasheet.cls) working with text mate, you have to:
1) Set the TEXINPUTS Shell Variable in preferences to .:..: (see Makefile.inc)
2) Typeset ( ⌘R ) 3 times to get the warnings to go away (see Makefile)
